:orphan:

Layer Operations (moved)
========================

This page has been split into category sub-pages under
:doc:`layer-ops/index` as part of the nnVLA layer port. Please see the
new index for activations, normalisation, linear / conv, attention,
DAG / skip, pooling / shape, embeddings, and specialty heads.
